type LinksProps = {
  linkName: string;
  isArrow?: boolean;
  toggleMenu?: boolean;
  linkSections?: string[];
};

type SliderProps = {
  heading: string;
  img: string;
}[];

type CustomButtonProps = {
  customName: string;
  customEffect: string;
};
